[project]
name = "week05-api-gallery-hub"
version = "0.1.0"
description = "API Gallery Hub — Week04 × Week05 火力展示版"
requires-python = ">=3.11"
dependencies = [
    "flask",
    "requests",
    "pytest",
]
